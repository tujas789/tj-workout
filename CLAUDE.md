# CLAUDE.md — โปรเจกต์ TJ Workout

## 0. อ่านก่อนตอบทุกครั้ง
- `README.md` — สารบัญ + ทำถึงไหนแล้ว
- `docs/SCOPE.md` — ขอบเขต MVP
- `CONTEXT.md` — คำศัพท์ (ช่องว่างกล้ามเนื้อ–เอ็น, แรงตก, วันทดสอบ, โดสขั้นต่ำ ฯลฯ)
- `docs/SCHEMA.md` — โครงชีต
- `docs/adr/` — การตัดสินใจที่ย้อนกลับยาก

## 1. โปรเจกต์โดยย่อ
เว็บแอปตารางฝึกส่วนตัวแบบบันทึกได้ (แบดมินตัน + กล้ามเนื้อ/เอ็น)
**Stack:** HTML/JS + Apps Script API + Google Sheet · **Deploy:** GitHub Pages

## 2. ผู้ใช้
คนเดียว — เจ้าของ (ชาย 38 · 177 cm · 60 kg · มังสวิรัติกินไข่ ไม่กินนมวัว ไม่ใช้อาหารเสริม)
ใช้บนมือถือ ในยิมและข้างสนามแบด **สัญญาณมักไม่ดี** · ภาษาไทยเสมอ

## 3. หลักการสำคัญ
1. **หัวใจ = กดบันทึกแล้วต้องติดทันที** — เขียน localStorage ก่อนเสมอ แล้วค่อย sync
2. **นาฬิกาคือเหตุผลที่แอปนี้มีอยู่** — ถ้าฟีเจอร์ไหนเอกสารทำแทนได้ ให้ตั้งคำถามว่าควรมีไหม
3. **โปรแกรมอยู่ในชีต ไม่ฝังในโค้ด** — แก้โปรแกรมต้องไม่ต้อง deploy
4. **เอ็นสำคัญกว่ากล้ามเนื้อในโปรเจกต์นี้** — ฟีเจอร์ที่กระตุ้นให้ดันหนักเกินคือฟีเจอร์ที่ผิด

## 4. สิ่งที่ห้ามทำ
- ❌ เพิ่มฟีเจอร์เกิน `docs/SCOPE.md` โดยไม่ถาม
- ❌ เขียนหน้ากราฟในแอป — Google Sheet ทำให้ฟรีแล้ว
- ❌ ใส่ auth/token/LockService — ผู้ใช้คนเดียว ไม่มี race (ต่างจาก TJ Inventory)
- ❌ ตั้ง `Content-Type` ตอน fetch ไป Apps Script — จะเกิด CORS preflight แล้วพัง
- ❌ ลืม bump `?v=YYYYMMDD` ใน `index.html` หลังแก้ css/js

## 5. รูปแบบการตอบ
ภาษาไทย กระชับ · จะแก้ไฟล์ → บอก file:line ก่อน · มีหลายทางเลือก → ใช้ AskUserQuestion

## 6. โครงไฟล์
- `frontend/` — source of truth ของหน้าเว็บ (copy ไป repo `tj-workout` แล้ว push)
  · `index.html` โครง · `instrument-panel.css` ธีม · `style.css` ชั้นบน (accent น้ำเงิน)
  · `program.js` นิยามโปรแกรม (สำรองของชีต) · `api.js` API + คิวออฟไลน์ · `app.js` logic + นาฬิกา
  · **ลำดับ script สำคัญ:** program → api → app และต้องอยู่ท้าย body
- `gas/working/` — Apps Script (push ด้วย clasp)
